{
  "name": "personal-group-board",
  "private": true,
  "license": "CC-BY-NC-4.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @board/web",
    "build": "npm run build --workspace @board/web",
    "test": "node scripts/validate.mjs && npm run build"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
